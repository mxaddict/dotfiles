#!/usr/bin/env bash
# vi: ft=bash

pkill pikr && exit 0

moji=$(pikr -d -p Emoji <"$HOME/.local/share/emojis" | cut -d ' ' -f 1 | tr -d '\n')

if [[ -n "$moji" ]]; then
    echo -n "$moji" | wl-copy

    wtype "$moji"
fi
