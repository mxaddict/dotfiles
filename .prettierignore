# Vendored skills (upstream, don't reformat)
.agents/**

# Vendored / generated
# Windows Terminal rewrites its settings in its own format on every change made
# in its settings UI, and `krypt adopt` copies that back as-is.
AppData/Local/Packages/Microsoft.WindowsTerminal_8wekyb3d8bbwe/LocalState/settings.json
.config/nvim/lazy-lock.json
.config/opencode/package-lock.json
.config/opencode/bun.lock
**/node_modules/**

# Long-form caches not meant to be re-formatted
.claude/**
.claude-work/**
.codex/projects/**

# Vieb tracked colors only — leave upstream untouched
.config/Vieb/colors/**
